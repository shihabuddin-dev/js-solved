/**  problem-14
Create two arrays of your choice.
Then combine the two arrays into a new array.
Print both the original arrays and the combined array. **/
let topBrand = ['Apple', 'Microsoft', 'Google', 'Samsung', 'Dell'];
let normalBrand = ['Lenovo', 'Xiaomi', 'Asus', 'One', 'Lava'];
// combineBrand = topBrand.concat(normalBrand);
console.log(topBrand);
console.log(normalBrand);
console.log(topBrand.concat(normalBrand));